enum VisitorStatus
{
  CheckedIn,
  CheckedOut
}
enum Gender
{
  Male,
  Female
}
enum ReasonForVisit
{
  Delivery, Meeting, Maintenance, Personal
}
enum RoomStatus
{
  Available, Occupied, Maintenance
}
enum RoomType
{
  Suite,
  Standard,
  Conference
}